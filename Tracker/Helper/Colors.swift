import UIKit

extension UIColor {
    
    static var color0: UIColor {
        guard let color = UIColor(named: "0") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color1: UIColor {
        guard let color = UIColor(named: "1") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color2: UIColor {
        guard let color = UIColor(named: "2") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color3: UIColor {
        guard let color = UIColor(named: "3") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color4: UIColor {
        guard let color = UIColor(named: "4") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color5: UIColor {
        guard let color = UIColor(named: "5") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color6: UIColor {
        guard let color = UIColor(named: "6") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color7: UIColor {
        guard let color = UIColor(named: "7") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color8: UIColor {
        guard let color = UIColor(named: "8") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color9: UIColor {
        guard let color = UIColor(named: "9") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color10: UIColor {
        guard let color = UIColor(named: "10") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color11: UIColor {
        guard let color = UIColor(named: "11") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color12: UIColor {
        guard let color = UIColor(named: "12") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color13: UIColor {
        guard let color = UIColor(named: "13") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color14: UIColor {
        guard let color = UIColor(named: "14") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color15: UIColor {
        guard let color = UIColor(named: "15") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color16: UIColor {
        guard let color = UIColor(named: "16") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    static var color17: UIColor {
        guard let color = UIColor(named: "17") else {
            assertionFailure("Unable to load color")
            return UIColor.white
        }
        return color
    }
    
}

let colorDictionary: [String: UIColor] = [
        "0": UIColor.color0,
        "1": UIColor.color1,
        "2": UIColor.color2,
        "3": UIColor.color3,
        "4": UIColor.color4,
        "5": UIColor.color5,
        "6": UIColor.color6,
        "7": UIColor.color7,
        "8": UIColor.color8,
        "9": UIColor.color9,
        "10": UIColor.color10,
        "11": UIColor.color11,
        "12": UIColor.color12,
        "13": UIColor.color13,
        "14": UIColor.color14,
        "15": UIColor.color15,
        "16": UIColor.color16,
        "17": UIColor.color17
    ]
